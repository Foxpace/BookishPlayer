enum BackupValidationFailure {
  unsupportedVersion,
  duplicateBook,
  duplicateMetadata,
  duplicateNote,
  duplicateSession,
  missingReference,
  invalidTheme,
  invalidBook,
  invalidNote,
  invalidSession,
}
